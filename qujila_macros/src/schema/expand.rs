use proc_macro2::TokenStream;
use super::model::{Schema, Field};

impl Schema {
    pub(super) fn expand(&self) -> TokenStream {
        todo!()
    }
}

impl Field {
    pub(super) fn expand(&self) -> TokenStream {
        todo!()
    }
}
